// import { useState } from "react";

// export function useLocalStorageItem(initialValue, key, callback) {
//   const [value, setValue] = useState(initialValue);
//   const handleAddWatch = (movie) => {
//     setWatchedList((watchedList) => [...watchedList, movie]);
//     localStorage.setItem(key, movie);
//   };
//   return initialValue ? setValue(localStorage.getItem(key)) : [];
// }
